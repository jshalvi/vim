snippet docready
	document.addEventListener('readystatechange', function(e) {
		if (document.readyState === 'complete') {
			${1}	
		}
	});

snippet for
	var ${2:i}, ${3:len}, ${4:item};
	for ($2 = 0, $3 = ${1:arr}.length; $2 < $3; $2++) {
		$4 = $1[$2];${5:}
	}
